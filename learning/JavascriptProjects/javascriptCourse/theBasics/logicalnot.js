// logical NOT (!)

let user = false

// The if statement will not run if the value is false.  This is why you use not, to turn the false user into a 'not'false user.
if(!user){
    console.log('User not logged in');
}